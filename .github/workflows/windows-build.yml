# =============================================================================
# SecureCast OBS Plugin — Windows 빌드 워크플로우 (Official Template 기반)
#
# 목적: Mac 개발자가 클라우드에서 Windows용 DLL을 빌드
# 해결: OBS 공식 템플릿의 워크플로우를 직접 호출하여 완벽한 SDK 환경 구축
# =============================================================================
name: Build

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - 'main'
  workflow_dispatch:

jobs:
  build-project:
    name: Build Project 🧱
    uses: obsproject/obs-plugintemplate/.github/workflows/build-project.yaml@master
